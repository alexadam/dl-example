#include <math.h>
#include "lib.h"

float file(char * name, int pos) {
	return 4.0;
}

float getLibVal(char * input, int index) {
	return ((int)(((21 * 3) + 1.73 )* pow(3,2)) % 8 ) / 5.55 + file("AAA", 0);
}

